//reverse a string in js 

function revfunvc()
{
    reverse = 0
    for (let c = revfunvc.length(); c <= 0; c++){
        reverse = reverse + c[char]
        
    }
    return reverse
    
}

ans = revfunvc("this is a string")
console.log(ans);
